const initState = {
    all:[],
    action: [],
    thriller: [],
    romance: [],
    horror: [],
}

const discoverReducer = (state = initState, action) => {
    return state;
}

export default discoverReducer;